interface:
  display_name: "Palisade"
  short_description: "Repo-specific patterns and workflows for palisade"
  default_prompt: "Use the palisade repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true